# triage-ai
Foundation for EMS triage extraction: **LLM → JSON → features** for a decision tree.

## Quickstart
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pytest -q
